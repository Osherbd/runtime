name: 'official plugin with jwt'
on: push
jobs:
  fetch_secrets:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Fetch static secrets from Akeyless
        uses: akeyless-community/akeyless-github-action@v1.1.6
        id: fetch-secrets
        with:
          access-id: p-sq9mxbj4flltos
          access-type: jwt
          api-url: http://ec2-18-118-131-65.us-east-2.compute.amazonaws.com:8000/api/v2
          static-secrets: |
            - name: "/asd"
              output-name: "my_secret"
